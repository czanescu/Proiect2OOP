#ifndef TYPES_H
#define TYPES_H

enum class DirectieX
{
    LEFT,
    RIGHT,
    NONE
};

enum class DirectieY
{
    UP,
    DOWN,
    NONE
};

enum class MenuSelection
{
    EXIT,
    CONTINUE
};

#endif // TYPES_H